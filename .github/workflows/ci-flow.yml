name: Humanitec-testing

on:
  push:
    branches: ["main"]
  pull_request:
    branches: ["main"]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3
      - name: "ls"
        run: ls
      - name: "change dir to client"
        run: cd client
      - name: "clent ls"
        run: ls
     
